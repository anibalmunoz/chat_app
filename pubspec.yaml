name: chat_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  pull_to_refresh_flutter3: ^2.0.1
  provider: ^6.0.5
  http: ^0.13.5
  flutter_secure_storage: ^8.0.0
  socket_io_client: ^2.0.1

  api_module:
    git:
      url: https://github.com/anibalmunoz/module-api-manager.git
      ref: main

  # api_module:
  #   path: D:/GitHub/module-api-manager

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

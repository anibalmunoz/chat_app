name: chat_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  pull_to_refresh_flutter3: ^2.0.1
  provider: ^6.0.5
  http: ^0.13.5

  # api_module:
  #   git:
  #     url: git@github.com:SegurosUniversales/mobile-module-api.git 
  #     ref: main

  api_module:
    path: D:/GitHub/module-api-manager
  flutter_secure_storage: ^8.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
